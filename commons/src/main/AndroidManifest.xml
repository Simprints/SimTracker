<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />

        <activity
            android:name="org.dhis2.commons.featureconfig.ui.FeatureConfigView"
            android:exported="false"/>

        <activity android:name="org.dhis2.commons.dialogs.imagedetail.ImageDetailActivity"
            android:exported="false" />

        <!-- These activities serve as Android-specific data transporters between this SimTracker and the separate Simprints ID app. Not part of UI. -->
        <activity android:name=".simprints.repository.flows.SimprintsBiometricEnrollmentRepository$SimprintsBiometricEnrollmentHandlingActivity" />
        <activity android:name=".simprints.repository.flows.SimprintsBiometricVerificationRepository$SimprintsBiometricVerificationHandlingActivity" />
        <activity android:name=".simprints.repository.flows.SimprintsBiometricIdentificationRepository$SimprintsBiometricIdentificationHandlingActivity" />

    </application>

</manifest>
